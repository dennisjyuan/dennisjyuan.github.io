source "https://rubygems.org"

# Matches what GitHub Pages runs, so `bundle exec jekyll serve` previews
# the site the same way GitHub builds it.
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"
